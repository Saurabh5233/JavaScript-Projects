<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShortUrl-Test app</title>
    <link rel="stylesheet" href="styles.css">

</head>

<body>
    <h1>Here you can shorten your URLs......</h1>
    <% if (locals.id) { %>
        <p>Url Generated : http://localhost:8001/url/<%= id %>
        </p>
        <% } %>
            <div>
                <form action="/url" method="post">
                    <h2><label for="redirectUrl">Enter your Url</label></h2>
                    <input type="text" name="url" placeholder="http://example.com/">
                    <button type="submit" name="redirectUrl">Generate</button>
                </form>
            </div>

            <% if (locals.urls) { %>
                <table>
                    <thead>
                        <th>S.No</th>
                        <th>Short Id</th>
                        <th>redirectUrl</th>
                        <th>Clicks</th>
                    </thead>
                    <tbody>
                        <% urls.forEach((url,index)=> { %>
                            <tr>
                                <td>
                                    <%= index+1 %>
                                </td>
                                <td>
                                    <%= url.shortUrl %>
                                </td>
                                <td>
                                    <%= url.redirectUrl %>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>

                            <% }) %>
                    </tbody>
                </table>
                <% } %>


</body>

</html>